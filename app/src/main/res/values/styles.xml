<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorBg</item>
        <item name="colorPrimaryDark">@color/colorBg</item>
        <item name="colorAccent">@color/colorBg</item>
        <item name="android:statusBarColor">@color/colorBg</item>
        <item name="android:navigationBarColor">@color/colorBg</item>
    </style>

    <!-- Estilo para los EditText del formulario -->
    <style name="EditTextLogin" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_background</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">@color/colorTen</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>